# git5 - 開發紀錄 v3.7

**日期**: 2026-05-04
**版本**: v3.7.0
**狀態**: 完成

## 開發摘要

本版本實作 mv 和 am 指令。

## 實作細節

### 1. 新增 mv 指令

在 `src/main.rs` 新增：
```rust
Mv {
    source: String,
    dest: String,
},
```

在 `src/commands.rs` 新增 `mv()` 函式：
- 支援 `git5 mv <source> <dest>` - 移動/重新命名檔案

### 2. 新增 am 指令

在 `src/main.rs` 新增：
```rust
Am {
    patch: String,
},
```

在 `src/commands.rs` 新增 `am()` 函式：
- 支援 `git5 am <patch>` - 套用 mbox 格式 patch

## 測試結果

- 整合測試: 21 passed
- 單元測試: 40 passed
- 總計: 61 passed

## 下一步

v3.8 規劃：
- 實作 format-patch 指令
- 實作 send-email 指令
- 更多錯誤處理優化