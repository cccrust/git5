# git5 - 開發紀錄 v3.6

**日期**: 2026-05-04
**版本**: v3.6.0
**狀態**: 完成

## 開發摘要

本版本實作 shortlog 和 whatchanged 指令。

## 實作細節

### 1. 新增 shortlog 指令

在 `src/main.rs` 新增：
```rust
Shortlog,
```

在 `src/commands.rs` 新增 `shortlog()` 函式：
- 支援 `git5 shortlog` - 顯示作者 commit 統計

### 2. 新增 whatchanged 指令

在 `src/main.rs` 新增：
```rust
Whatchanged,
```

在 `src/commands.rs` 新增 `whatchanged()` 函式：
- 支援 `git5 whatchanged` - 顯示每個 commit 變更的檔案

## 測試結果

- 整合測試: 21 passed
- 單元測試: 40 passed
- 總計: 61 passed

## 下一步

v3.7 規劃：
- 實作 mv 指令
- 實作 am 指令
- 更多錯誤處理優化